[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[project]
name = "compareexpressions-evaluation-result"
version = "0.1.0"
description = "Evaluation result container extracted from compareExpressions."
requires-python = ">=3.11"
dependencies = []

[tool.poetry]
packages = [{ include = "compareexpressions/evaluation_result", from = "src" }]
