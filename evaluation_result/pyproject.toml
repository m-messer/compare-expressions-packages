[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "compareexpressions-evaluation-result"
version = "0.1.0"
description = "Evaluation result container extracted from compareExpressions."
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["evaluation_result*"]
